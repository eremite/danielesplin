!!! XML
%rss{:version => '2.0'}
  %channel
    %title Nutritional Bites
    %link= root_url
    %description
      Bites of deliciously referenced nutritional information.
    %language en-us
    %pubDate= Time.now.rfc822

    - @nutritional_posts.each do |post|
      %item
        %title= post.title
        %description
          %h3= post.title
          #{post.bite.html_safe}
        %link= public_nutritional_post_path(post)
        %guid= public_nutritional_post_path(post)
        %pubDate= post.created_at.rfc822
