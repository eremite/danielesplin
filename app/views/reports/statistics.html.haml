- title 'Statistics'

%dl.dl-horizontal

  %dt Daniel's Entries
  %dd= number_with_delimiter User.where(role: 'father').first.entries.private.count

  %dt Erika's Entries
  %dd= number_with_delimiter User.where(role: 'mother').first.entries.private.count

  %dt Blog Posts
  %dd= number_with_delimiter Entry.public.count

  %dt Photos
  %dd= number_with_delimiter Photo.count
