- title 'Categories'

%ul
  - @saved_file_categories.each do |category|
    %li
      = category.name
      &ndash;
      = link_to category, method: :delete, data: { confirm: 'Are you sure you want to delete this category?' } do
        delete

.row.span12
  = form_for @saved_file_category, class: 'form-inline' do |f|
    = f.text_field :name, placeholder: f.object.class.human_attribute_name(:name), class: 'autofocus'
    %button.btn.btn-primary{ type: 'submit' } Add Category
