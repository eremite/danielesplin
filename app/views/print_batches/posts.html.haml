%h1
  Blog
  = params[:year]

- @posts.each do |post|
  %h3{ style: 'float: right' }= l post.at
  :markdown
    #{post.body}
  - post.photos.at_asc.each do |photo|
    %figure{ style: 'float: left; margin: 0 0.25em' }
      = image_tag photo.crop_url(width: 200, height: 200)
      %figcaption{ style: 'min-height: 1.5em; width: 200px' }
        = photo.description
  %br{ :style => 'clear: both' }
