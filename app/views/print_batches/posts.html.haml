%h1
  Blog
  = params[:year]

- @posts.each do |post|
  %h3{ style: 'float: right; font-size: smaller' }= l post.at
  :markdown
    #{post.body}
  - post.photos.at_asc.each do |photo|
    %figure{ style: 'float: left; margin: 0 0.25em' }
      = image_tag photo.resize_url(:medium), :width => '400px'
      %figcaption{ style: 'min-height: 1.5em; width: 400px' }
        = photo.description
  %br{ :style => 'clear: both' }
