- title 'Comments'


- if @comments.empty?
  %p No comments.
- else
  - @comments.each do |comment|
    %h3= link_to comment.post.title, comment.post
    = render comment

%p.clearfix
  = paginate @comments
