- title 'Blog', false

- @posts.each do |post|
  .mb-4
    %h5.float-end.text-reset
      = l(post.at.to_date, format: :long)
    %h1.text-reset
      = link_to post.title, :posts
    - if post.photos.present?
      Includes #{pluralize(post.photos.count, 'picture')}.

= paginate @posts
