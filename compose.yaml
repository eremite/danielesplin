services:

  web:
    build: .
    volumes:
      - .:/rails
    ports:
      - "80:3000"
    environment:
      - FROM_EMAIL=test@example.com
      - PHOTO_FRAME_IP=127.0.0.1
      - GEMINI_API_KEY=${GEMINI_API_KEY}
