#!/usr/bin/env sh

echo "To get a Rails console:"
printf "  sudo su\n  cd /var/app/current\n  rails console\n"
sudo chmod 600 .ssh.key
bin/eb ssh --custom 'ssh -i /app/.ssh.key -o StrictHostKeyChecking=no' "$@"
