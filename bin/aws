#!/usr/bin/env sh

export AWS_DEFAULT_REGION=us-east-1

command="aws"
if [ "$1" = "eb" ]; then
  shift
  command="eb"
fi

docker run --rm -it \
  -v "$PWD:/app" -w "/app" \
  -e AWS_ACCESS_KEY_ID \
  -e AWS_SECRET_ACCESS_KEY \
  -e AWS_DEFAULT_REGION \
  eremite/aws-cli:2018.12.31 "$command" "$@"
